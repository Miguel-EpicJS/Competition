#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int t;

    cin >> t;

    while(t--)
    {

	int a, b, c;
	long long k;

	cin >> a >> b >> c >> k;
	int m = pow(10, a);
	int j = 1;
	bool ok = true;
	for (int i = pow(10, a-1); i < m; i++)
	{
	    int kb = 0; 
	}

    }

    return 0;
}

