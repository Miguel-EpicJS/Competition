#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int t = 1;

    cin >> t;

    while(t--)
    {
	int a, b, c, x, y, z, n, m, k;

	cin >> n;

	vector<int> arr1, arr2;

	for (int i = 0; i < n; i++)
	{
	    cin >> x;
	}
    }


    return 0;
}

