#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int t;

    cin >> t;

    while(t--)
    {
	int x, y, z, k, w, a, b, c, n, m;

	cin >> n;

	vector<int> arr;

	for (int i = 0; i < n; i++)
	{
	    cin >> x;
	    arr.push_back(x);
	}
    }

    return 0;
}

