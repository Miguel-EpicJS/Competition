#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int t;

    cin >> t;

    while(t--)
    {

	long long a, b, c, n, m, k;

	cin >> n;

	vector<long long> arr;
	for (int i = 0; i < n; i++)
	{
	    cin >> a;
	    arr.push_back(a);
	}

    }

    return 0;
}

