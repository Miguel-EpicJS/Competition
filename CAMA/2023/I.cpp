#include <bits/stdc++.h>

using namespace std;

int arr[1010][1010];

int dx[4] = {};

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int t;

    cin >> t;
    
    while(t--)
    {
	memset(arr, 0, sizeof(arr));
	int n, m;

	cin >> n >> m;

	

	for (int i = 0; i < m; i++)
	{
	    int x, y;
	    char k;
	    cin >> x >> y >> k;


	}
	
    }

    return 0;
}

