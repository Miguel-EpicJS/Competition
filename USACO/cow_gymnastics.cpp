#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    int k, n;

    cin >> k >> n;

    vector<int> first(n);
    int wins[21][21];

    for (int i = 0; i < n; i++)
    {
	cin >> first[i];
    }

    

    return 0;
}

