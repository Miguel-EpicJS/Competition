#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n;

    cin >> n;

    vector<tuple<int, int, char>> sort_x;

    for (int i = 0; i < n; i++)
    {
	char x;
	int y, z;
	cin >> x >> y >> z;
	sort_x.push_back({y, z, x});
    }

    vector<long long> mins(n+1, INT_MAX);

    for ()

    return 0;
}

